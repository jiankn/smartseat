{
  "buildCommand": "pnpm -F @smartseat/types build && pnpm -F @smartseat/db generate && pnpm -F @smartseat/utils build && pnpm -F @smartseat/web build",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nextjs",
  "outputDirectory": "apps/web/.next"
}
